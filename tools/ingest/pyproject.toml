[project]
name = "ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "python-frontmatter>=1.1.0",
    "python-dotenv>=1.0.0",
]

# Run with: python -m ingest <command>
# Install with: pip install -e tools/ingest
